# Backend configuration
API_HOST=0.0.0.0
API_PORT=8000

# Add provider credentials here for local development.
GROQ_API_KEY=

# Frontend configuration — where the Next.js app looks for the FastAPI backend.
# Falls back to precomputed data (see frontend/lib/scenarios.ts) if unreachable.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
